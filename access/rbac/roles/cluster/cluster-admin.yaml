apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: cluster-admin
  labels:
    rbac.bankingapp.io/role: admin
    rbac.bankingapp.io/scope: cluster
    rbac.bankingapp.io/managed-by: gitops
rules:
  # Full access to all resources
  - apiGroups: ["*"]
    resources: ["*"]
    verbs: ["*"]

