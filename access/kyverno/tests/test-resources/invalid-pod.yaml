apiVersion: v1
kind: Pod
metadata:
  name: test-violate-semantic-version
  namespace: default
spec:
  securityContext:
    runAsNonRoot: true
    runAsUser: 1000
  containers:
  - name: test-container
    # INVALID: Uses 'latest' tag
    image: 063630846340.dkr.ecr.us-east-1.amazonaws.com/accounts:latest
    securityContext:
      runAsNonRoot: true
      runAsUser: 1000
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 512Mi