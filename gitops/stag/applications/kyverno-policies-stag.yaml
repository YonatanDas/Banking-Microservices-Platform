apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kyverno-policies-stag
  namespace: argocd
spec:
  project: banking-stag-project
  source:
    repoURL: https://github.com/yonatandas/Multi-Environment-Microservices.git
    targetRevision: main
    path: kyverno/policies/cluster
  destination:
    server: https://kubernetes.default.svc
    namespace: kyverno
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - ServerSideApply=true
      - PrunePropagationPolicy=foreground
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

